When asked to "sync config" or "update config":
1. Run `git -C ~/Work/claude-config pull` to get the latest
2. Read all files in ~/Work/claude-config/ (CLAUDE.md, preferences.md, and any other config files)
3. Apply `global/CLAUDE.md` to `~/.claude/CLAUDE.md`
4. Read `preferences.md` and apply any preferences to memory
5. Confirm what was updated
