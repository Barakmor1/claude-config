Back up Claude Code configuration.

1. Run `~/Work/claude-config/backup.sh`
2. Run `git -C ~/Work/claude-config diff` to show what changed
3. If there are changes, commit and push to origin
4. If no changes, just say "Config backup is up to date"
